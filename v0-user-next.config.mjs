/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable static optimization for pages without server-side data requirements
  reactStrictMode: true,
  swcMinify: true,
};

export default nextConfig;

